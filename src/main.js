import fullReplace from "./fullReplace";

/**
 * Grabs HTML from supplementary article and replaces it.
 *
 * @callback  a function to be called afterwards
 */
export const fullReplace = callback => {
  fullReplace(callback);
};

// TODO: Add other functions like one that returns an array of named nodes for Colin
